# CONTEXT — Read Frog 精简优化项目

> 生成于 2026-06-19，通过 `/grill-with-docs` 深度访谈

## 项目目标

基于 Read Frog（陪读蛙）开源浏览器扩展，精简为**纯翻译工具**，优化上下文翻译精准度，简化 UI。

## 核心使用场景

用户打开任意语言网页 → 自动/手动触发双语对照翻译（原文一段 + 译文一段），类似沉浸式翻译的核心体验。语言对不限。

## 功能决策

### ✅ 保留

| 功能 | 说明 |
|------|------|
| 双语页面翻译 | 核心功能，bilingual 模式 |
| 右键菜单翻译 | 右键 → "翻译此页面" |
| 自定义翻译样式 | 翻译文字样式（字体、颜色等） |
| 翻译范围选择 | 整页 / 可见区域 |
| 页面翻译快捷键 | 如 Alt+T |
| 节点翻译热键 | 悬停+热键翻译段落 |

### ❌ 砍掉

| 功能 | 原因 |
|------|------|
| 视频字幕翻译 | 不需要 |
| 划词翻译 | 不需要 |
| 输入框翻译 | 不需要 |
| TTS 语音朗读 | 不需要 |
| 统计页面 | 不需要 |
| 浮动按钮 | 不需要 |
| 自定义操作 (Custom Actions) | 深度学习功能，不需要 |
| Translation Hub | 独立翻译工作台，不需要 |
| Side Panel 侧边栏 | 占位代码，不需要 |

### 设置页面

保留：通用设置、API 提供商、翻译设置、右键菜单、配置

砍掉：自定义操作、视频字幕、浮动按钮、划词工具栏、输入框翻译、TTS、统计

### 翻译设置配置项

保留所有现有配置项，移除以下：
- AI 上下文感知开关 → 改为术语表模式（始终开启，无需开关）
- 个性化提示词 → 用默认 prompt 即可，砍掉自定义能力
- 翻译模式 → 只保留 bilingual

## 上下文翻译优化方案

### 两阶段术语表模式

**第一阶段：术语提取**
- AI 通读全文内容
- 自动判断并提取**所有需要统一译法的词**
- 输出格式：`{原文术语} → {推荐中文译法}` 列表
- 不限制数量上限，由 AI 自动判断

**第二阶段：带术语表翻译**
- 将术语表注入 system prompt
- 逐段翻译，确保全文术语一致性、多义词正确消歧

**设计约束**：尽量省 token。第一阶段只输出术语表（短文本），不输出完整摘要。

## UI 方向

- **风格**：Chrome 浏览器原生简洁风格（小字号、紧凑布局、低视觉噪音）
- **策略**：在现有界面上做减法 + 样式调整，不改架构
- **Popup**：保留当前所有元素，默认行为不变
- **参考**：`chrome://settings` 风格

## 技术栈（不变）

- WXT 浏览器扩展框架
- React + TypeScript
- Jotai 状态管理
- pnpm 包管理
- 20+ AI 提供商（通过 AI SDK）
- Tailwind CSS

## 改动范围

分为几个阶段推进：

1. **精简砍功能** — 移除不需要的 entrypoint、组件、设置页
2. **上下文翻译优化** — 实现术语表两阶段翻译
3. **UI 简化** — 样式调整，Chrome 原生风格
4. **收尾清理** — 移除相关常量、类型、locale 文本

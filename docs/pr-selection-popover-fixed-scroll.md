## Type of Changes

- [x] 🐛 Bug fix (fix)
- [x] 📝 Documentation change (docs)

## Description

Fix the shared selection popover so it stays fixed in the viewport after opening instead of moving with page scroll:

- **Viewport host**: Render the popover inside a fixed full-screen host so page scrolling no longer shifts the floating card.
- **Existing layout behavior preserved**: Keep the current drag, resize, viewport clamping, and bottom-edge growth logic intact by leaving the `react-rnd` card inside the new fixed host.
- **Regression coverage**: Add a unit test asserting the fixed host is used and scroll does not change the popover position.
- **Agent skill docs**: Update the `prepare-pr` and `create-pr` skills so they explicitly allow running without an issue argument.

## Related Issue

N/A

## How Has This Been Tested?

- [x] Added unit tests

```bash
pnpm vitest run src/components/ui/selection-popover/__tests__/selection-popover.test.tsx
```

## Screenshots

N/A

## Checklist

- [x] I have tested these changes locally
- [x] I have updated the documentation accordingly if necessary
- [x] My code follows the code style of this project
- [x] My changes do not break existing functionality
- [x] If my code was generated by AI, I have proofread and improved it as necessary.

## Additional Information

The popover fix keeps the existing `useSelectionPopoverLayout` viewport math unchanged. The structural change is limited to wrapping `SelectionPopoverShell` in a fixed `DialogPrimitive.Popup` host, which preserves the existing drag boundary and streaming growth behavior already covered by the selection popover test suite.

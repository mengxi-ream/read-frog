---
"@read-frog/extension": minor
---

feat: add PDF translation with bilingual overlay

Adds a built-in PDF viewer that renders PDF documents using PDF.js and
overlays inline translations on each text block. When enabled in the
options page, navigating to a `.pdf` URL automatically opens the document
in the Read Frog PDF viewer with page-by-page translation support.

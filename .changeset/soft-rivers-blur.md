---
"@read-frog/extension": patch
---

feat(subtitles): add hover-reveal blur option for translated subtitles

Adds `videoSubtitles.style.blurTranslation` (default: false). When enabled, translations appear blurred and reveal on hover; blur re-applies when the cue changes.
